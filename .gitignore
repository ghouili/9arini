/node_modules
/.next
/out
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Uploaded verification documents (sensitive — never commit)
/.storage

# Env / secrets — keep only .env.example tracked
.env
.env.*
!.env.example

# UI-audit screenshots — regenerated by `node scripts/ui-audit/shots.mjs`
scripts/ui-audit/shots/

# UI-audit baselines
scripts/ui-audit/shots-*/
