# Phase A · Lane L6 · Legal text (A19) + Derija sweep

```
BATCH 6 — Make /privacy and /terms true (A19), plus the Derija to-do from lane L5
Branch phase-a/l6, cut from aafc699 (launch-hardening after the L1–L5 merges). DB tnajem_test_l6 (--fresh, npm run db:sql: 31 files).
Baseline before: npm run test:api → # tests 542 · # suites 126 · # pass 542 · # fail 0
Full suite after: npm run test:api → # tests 567 · # suites 130 · # pass 567 · # fail 0 · typecheck exit 0 · lint exit 0
```

| ID | Finding | Status | Commit |
|---|---|---|---|
| A19 | CEO report §4 (5 sentences) + everything Batches 1–5 changed | DONE | `d998693` |
| A18.derija-2 | L5's "MSA still present in OTHER lanes' files" table | DONE | `23ddd30` |

BLOCKED: none. NOT-REPRODUCED: none.

---

## A19 · CEO report §4 · DONE · commit `d998693`

Pages: `apps/web/app/[locale]/privacy/page.tsx`, `apps/web/app/[locale]/terms/page.tsx` (FR + AR). The pages stay client
components rendered from a `{ fr, ar }` copy object, so they stay prerendered (`e2e/static-render.spec.ts` needs `x-nextjs-cache: HIT`).
One addition to the page-local `Section` type: `story?: { audience, note }`, rendered by the shared `<PaymentStory>` (L3) so the one
payment sentence always carries its visible « Bientôt » / « قريب » label.

REPRODUCE — `apps/api/test/legal-truth.test.ts` (new). It reads both page sources with `node:fs` (comments stripped, split into the
FR and AR halves) and asserts each old sentence gone / each new one present; its last block drives `POST /messages/:id/report`
through the real route (signed out, stranger, participant). The route block passes before the fix on purpose: it is the existing
behaviour the new sentence describes.

BEFORE (`cd apps/api && npx tsx --import ./test/test-env.ts --test test/legal-truth.test.ts`):
```
    not ok 1 - 1. a message is no longer reportable « sans compte »: it needs a participant's account
      error: 'privacy: still says « sans compte »'
    not ok 2 - 2. contact details: the whole number or handle is masked, messaging links included (A1)
      error: 'privacy: still says « Les coordonnées (numéro, email, lien) en sont retirées automatiquement »'
    not ok 3 - 3. every photo is erased, earlier versions included; a replaced photo is deleted (A11)
      error: 'privacy: still says « tes documents partagés et ta photo sont effacés du stockage »'
    not ok 4 - 4. a parent's name, e-mail and phone are erased from the child's consent too (A27)
      error: 'privacy: missing « ton nom, ton e-mail et ton téléphone sont aussi effacés de cet accord »'
    not ok 5 - 5. « nous pouvons retirer un contenu » now says what an admin can do (A28)
      error: 'terms: still says « nous pouvons retirer un contenu ou suspendre un compte »'
not ok 1 - A19 · the five sentences of CEO report §4
  error: '5 subtests failed'
    not ok 1 - the adult-only pilot, with birth month + year (A24, A14)
      error: 'privacy: missing « Pendant le pilote, Tnajem est réservé aux 18 ans et plus »'
    not ok 2 - conversations close — immediately on a block or a withdrawal, else THREAD_CLOSE_DAYS after the class (A2)
      error: 'privacy: the number comes from @tnajem/shared, not typed in: missing « THREAD_CLOSE_DAYS »'
    not ok 3 - students see a tutor as first name + initial (A23)
    not ok 4 - payments: one future story, labelled « Bientôt », and nothing else (A22, D4)
      error: 'privacy: the labelled sentence is rendered by the shared component: missing « <PaymentStory »'
    not ok 5 - the phone is for Tnajem's own messages, never shown to a tutor (A3)
      error: 'privacy: still says « te joindre »'
    not ok 6 - a guardian's phone is no longer collected (A18.2)
    not ok 7 - every sentence on retention, lawful basis or consent that changed carries a LEGAL-REVIEW marker
      error: 'privacy: only 0 LEGAL-REVIEW markers'
not ok 2 - A19 · what Batches 1–5 changed
  error: '7 subtests failed'
    ok 1 - signed out: refused; a stranger: not found; the other participant: filed
ok 3 - A19 · « un message se signale depuis sa conversation, par l'élève ou le prof qui y participe »
# tests 13
# pass 1
# fail 12
```
AFTER (same command; later also asserts that neither page says "SMS"):
```
    ok 1 - 1. a message is no longer reportable « sans compte »: it needs a participant's account
    ok 2 - 2. contact details: the whole number or handle is masked, messaging links included (A1)
    ok 3 - 3. every photo is erased, earlier versions included; a replaced photo is deleted (A11)
    ok 4 - 4. a parent's name, e-mail and phone are erased from the child's consent too (A27)
    ok 5 - 5. « nous pouvons retirer un contenu » now says what an admin can do (A28)
ok 1 - A19 · the five sentences of CEO report §4
    ok 1 - the adult-only pilot, with birth month + year (A24, A14)
    ok 2 - conversations close — immediately on a block or a withdrawal, else THREAD_CLOSE_DAYS after the class (A2)
    ok 3 - students see a tutor as first name + initial (A23)
    ok 4 - payments: one future story, labelled « Bientôt », and nothing else (A22, D4)
    ok 5 - the phone is for Tnajem's own messages, never shown to a tutor (A3)
    ok 6 - a guardian's phone is no longer collected (A18.2)
    ok 7 - every sentence on retention, lawful basis or consent that changed carries a LEGAL-REVIEW marker
ok 2 - A19 · what Batches 1–5 changed
    ok 1 - signed out: refused; a stranger: not found; the other participant: filed
ok 3 - A19 · « un message se signale depuis sa conversation, par l'élève ou le prof qui y participe »
# tests 13
# pass 13
# fail 0
```
`apps/api/test/payment-story.test.ts` (L3's A22 scan) excluded `/terms` and `/privacy` "until L6"; the exclusion is removed, so the
provider/other-story scan now covers both pages too: `ok 3 - no web copy tells another payment story, or names a provider` (4/4).

Gates after A19: `npm run typecheck` exit 0 · `npm run lint` exit 0 · `npm run test:api` `# tests 555 · # pass 555 · # fail 0`.

### The five §4 sentences — resolution and the passing test behind each

| §4: the page said | Now says (FR; AR mirrors it) | Backed by (all passing in the final run) |
|---|---|---|
| 1. Messages can be reported « sans compte » | Page, class, document: « avec le bouton « Signaler », même sans être connecté ». A message: « se signale depuis sa conversation, par l'élève ou le prof qui y participe, connecté à son compte — même quand la conversation est fermée ». If signed in, the report is tied to the account (role + account e-mail shown to admins, A18.14). The guardian's button (Dm2) is **not** mentioned | `legal-truth.test.ts` › "signed out: refused; a stranger: not found; the other participant: filed"; `messages-route.test.ts` › "A2 — a closed conversation stays readable and reportable" › "both parties read it, see it is closed, and can still report"; `l5-report-role.test.ts` › "the queue carries the reporter's role and account…"; e2e `messaging.spec.ts` › "an outsider cannot report a message…" |
| 2. Contact details are removed automatically | Kept, and precise: masked « en entier » before storage — a number (with or without +216), an e-mail, a link, a messaging link (wa.me, t.me, Facebook, Instagram) with the number/handle after it; the TYPE is kept, never the text; « Ce filtre n'attrape pas tout » (exchanging contacts stays forbidden). /terms: the same for reviews **and messages** | `contact-info.test.ts` › "A1 — maskContactInfo removes the whole handle or number" (the 10-row table + fixed-point invariant + false-positive guards) |
| 3. The photo is erased | « toutes tes photos — y compris les versions que tu avais remplacées — sont effacés du stockage »; « Quand tu remplaces ou supprimes ta photo, l'ancienne est effacée du stockage, dans ses trois tailles » | `avatar-erasure.test.ts` › "A11 — a replaced or deleted photo is deleted from storage", "A11 — erasure deletes EVERY photo the tutor ever uploaded" (3 uploads → 0 objects); `storage-prefix.test.ts` › "localStore.deletePrefix", "s3Store.deletePrefix (stubbed client)" |
| 4. Name and phone are erased | Erasure paragraph adds: the guardian named in a child's consent has « ton nom, ton e-mail et ton téléphone … aussi effacés de cet accord : il n'en reste que le fait qu'il a été donné, sa date, son texte et sa version » + a §5 retention line for consents. `// LEGAL-REVIEW: retention of consent proof` | `consent-erasure.test.ts` › "A27 — erasing a guardian anonymises them in their child's consent record" › "name, e-mail and phone are gone; the fact of consent, its date and its version stay" |
| 5. « Nous pouvons retirer un contenu » | « nous pouvons masquer un message ou un avis, retirer un document ou suspendre un compte »; a hidden message/review shows « Contenu retiré par la modération » to everyone (author included); its text is kept as evidence and only admins can read it; each removal is decided by a person, with a reason, and recorded. The strings are the ones readers get: the test imports `MODERATION_PLACEHOLDER` from `apps/api/src/lib/moderation-hide.ts` | `moderation-hide.test.ts` › "A28 — hiding a reported MESSAGE" (placeholder for the other party, audit row, admin still sees text; no reason → refused), "A28 — hiding a REVIEW" |

### Also changed on the pages (Batches 1–5), each with its backing test

| Behaviour | Where on the pages | Backed by |
|---|---|---|
| **Adult-only pilot** (A24/A14): 18+ for students and tutors while `ALLOW_MINORS` is off; birth **month + year**; majeur from the month after the 18th birthday (Tunis); unknown = minor; a tutor must always be 18+; an existing minor account signs in but cannot book | privacy lead, §2, §3, §8; terms §2. The minor/guardian passages are framed as « ne s'appliquent pas tant que cet accès est fermé »; /terms says only that the terms will be updated before minors are ever allowed | `a24-adult-pilot.test.ts` (A24 · sign-up refuses minors…, an existing minor can sign in but cannot book), `a14-tutor-age.test.ts`, `age.test.ts` › "isAdult (birth month + year, Africa/Tunis)" |
| **Conversations close** (A2): immediately on a cancelled booking, a suspended/blocked account, or a withdrawn consent; otherwise `THREAD_CLOSE_DAYS` (imported, = 7) after the end of the class; closed = readable + reportable, nobody can write | privacy §3 (messages) + §5 (closing deletes nothing); terms §8 + §14 | `messages-route.test.ts` › "A2 — a closed conversation refuses new messages", "A2 — … stays readable and reportable" |
| **First name + initial** (A23/D1): « ton prénom suivi de l'initiale de ton nom (« Sami B. ») »; full name seen only by the tutor and admins; the page address (slug, chosen by the tutor) is public | privacy §2, §4; terms §8 | `a23-tutor-name.test.ts` › "A23 · no student-facing endpoint sends the tutor's last name", "A23 · the tutor still sees their own full name" |
| **Payments, future only** (A22/D4): `<PaymentStory>` renders « [Bientôt] Tu paies en ligne, via Tnajem, avant la séance. » + « pas encore aujourd'hui »; no provider, card, wallet or e-dinar named; « Aucune carte… » and the « s'arranger financièrement en dehors de Tnajem » paragraph deleted (it was the pay-the-tutor-directly story D4 retired) | privacy §2; terms §6 | `payment-story.test.ts` › "A22 · the one payment story" (now scanning both pages); `legal-truth.test.ts` › payments test |
| **Phone** (A3): « il sert uniquement aux messages que Tnajem peut t'envoyer sur ton compte et tes séances … jamais montré à un prof ou à un élève, jamais à te connecter ». "te joindre" is gone. The channel (SMS) is deliberately not named: SMS is optional and off by default, and `e2e/privacy-documents.spec.ts` forbids "par SMS" | privacy §2, §3; terms §3 | `contact-info.test.ts` › "contactFieldPaths — the structural guard for whole payloads" (no contact field reaches the other party); e2e `zero-contact-copy.spec.ts` (A3 welcome copy) |
| **No guardian phone** (A18.2) | privacy §2, §3, §8; terms §2 | `l5-consent-phone.test.ts` › "a consent with no phone is accepted and stores no phone" |
| Guardian lock (A13): once linked, the child cannot change the guardian's address | privacy §8 | `a13-guardian-lock.test.ts` |
| Free first session once per student per tutor (A6): late student cancel spends it; an early cancel, a tutor cancel or a moved class gives it back | terms §5 | `free-first-once.test.ts` › "A6 · the free first session: once per student per tutor" |
| Moved class ⇒ nothing retained on a late cancel (A21) | terms §7 | `cancel-outcome.test.ts` › "a class the tutor MOVED after the booking, cancelled late: waived, nothing retained" |
| Approval needs the current round's declaration (A15); verified rename/resubmit (A26) | privacy §3; terms §9 | `approve-declaration.test.ts`, `verified-rename-resubmit.test.ts` |
| Documents attached to a class open to that class's students (A18.9) | privacy §3 | `l5-materials-class.test.ts` |
| ID-document clock runs from the **latest** decision (retention.ts; an earlier round waits for the newer round's decision) | privacy §5 (+ LEGAL-REVIEW) | `verified-rename-resubmit.test.ts` › "the 90-day purge never takes a round that is still under review" |

`// LEGAL-REVIEW:` markers added on every retention / lawful-basis / consent sentence touched (privacy: consent record, lawful bases,
age check, guardian consent, erasure retention, ID-document clock, message + hidden-content retention, consent retention, §8 consent;
terms: minors line, §6 payments clause question, hidden-content retention). No period or basis was invented: where the code sets
none, the page says what the code does and keeps « [Durée à fixer par l'avocat] ».

**Document versions:** `PRIVACY_POLICY_VERSION` and `TERMS_VERSION` (`packages/shared/src/legal.ts`) moved `2026-09-15 → 2026-09-24`,
so a consent or account recorded from now on points at the text it was given under (the pages show « Version du 24 septembre 2026 »).
Nothing enforces re-acceptance; revert is one line each if the founder prefers to bump only at publication.

Files touched: `apps/web/app/[locale]/privacy/page.tsx`, `apps/web/app/[locale]/terms/page.tsx`, `packages/shared/src/legal.ts`,
`apps/api/test/legal-truth.test.ts` (new), `apps/api/test/payment-story.test.ts` (exclusion removed), `e2e/legal-truth.spec.ts` (new),
`e2e/privacy-documents.spec.ts` (updated).

### Playwright
- **Written, not run** (the orchestrator runs it after merge): `e2e/legal-truth.spec.ts` — `/fr` and `/ar`, `/privacy` and `/terms`:
  the old sentences gone, the new ones present (with `THREAD_CLOSE_DAYS` substituted), no other payment story/provider, and every
  `[data-payment-story]` element is `soon` with its visible « Bientôt » / « قريب » label and holds every occurrence of the sentence.
- **Updated:** `e2e/privacy-documents.spec.ts` — « Version du 15 septembre 2026 » → « Version du 24 septembre 2026 » (version bump);
  test title "…reporting needs no account" → "…reporting a page needs no account". Every other assertion still holds against the new
  text (checked: `90 jours après la décision`, `Nous te laissons 30 jours…`, « effacés du stockage », « retirer son accord depuis son
  espace parent », « avec le bouton « Signaler » », no « par SMS », AR periods).
- **Checked, unaffected:** `page-metadata.spec.ts` (titles/OG untouched), `static-render.spec.ts` (still static client pages),
  `privacy-consent.spec.ts` (uses the version constants), `privacy-retention.spec.ts`, `privacy-decree.spec.ts`, `payment-story.spec.ts`
  (does not crawl the legal pages).

---

## A18.derija-2 · L5 to-do table · DONE · commit `23ddd30`

REPRODUCE — `apps/api/test/l6-derija.test.ts` (new): per file, each [MSA, Derija] pair; the MSA must be gone and the Derija present.

BEFORE:
```
    not ok 1 - app/[locale]/class/[id]/page.tsx
        +   'still MSA: هذه الحصة كاملة',
        +   'still MSA: هذه الحصة ما عادش مفتوحة للحجز',
        +   'still MSA: free: "مجانية"',
        +   'still MSA: الحصة الأولى مجانية ·',
        …
    not ok 2 - components/checkout/CheckoutInner.tsx
    not ok 3 - app/[locale]/live/[id]/page.tsx
    not ok 4 - app/[locale]/student/page.tsx
    not ok 5 - app/[locale]/dashboard/new-pack/page.tsx
    not ok 6 - components/WrongRoleNotice.tsx
    not ok 7 - app/[locale]/layout.tsx
    not ok 8 - app/[locale]/page.tsx
    not ok 9 - app/[locale]/signup/eleve/page.tsx
    not ok 10 - app/[locale]/student/layout.tsx
    not ok 11 - app/[locale]/privacy/page.tsx
    not ok 12 - app/[locale]/terms/page.tsx
not ok 1 - Derija sweep of other lanes' files (A18.derija-2)
# tests 12
# pass 0
# fail 12
```
AFTER (`l6-derija` + `l5-derija` + `legal-truth` together):
```
ok 1 - Derija — the Arabic is Tunisian
    ok 1 - app/[locale]/class/[id]/page.tsx
    … ok 2 … ok 11 …
    ok 12 - app/[locale]/terms/page.tsx
ok 2 - Derija sweep of other lanes' files (A18.derija-2)
ok 3 - A19 · the five sentences of CEO report §4
ok 4 - A19 · what Batches 1–5 changed
ok 5 - A19 · « un message se signale depuis sa conversation, par l'élève ou le prof qui y participe »
# tests 28
# pass 28
# fail 0
```
Gates after: typecheck exit 0 · lint exit 0 · `npm run test:api` `# tests 567 · # pass 567 · # fail 0`.

### Every change, before → after (AR values only; FR untouched, so `ar: typeof fr` parity is unchanged)

| File (owner) | Before | After |
|---|---|---|
| `class/[id]/page.tsx` (L4) | `soldOutTitle: "هذه الحصة كاملة"` | `"الحصة هاذي كاملة"` |
| 〃 | `closedTitle: "هذه الحصة ما عادش مفتوحة للحجز"` | `"الحصة هاذي ما عادش تتحجز"` |
| 〃 *(extra)* | `reassure: "الحصة الأولى مجانية · إلغاء مجاني حتى 48 ساعة قبل"` | `"أول حصة فابور · إلغاء مجاني حتى 48 ساعة قبل"` |
| 〃 *(extra)* | `free: "مجانية"` | `"فابور"` |
| `checkout/CheckoutInner.tsx` (L3) | `okTitle: "تم الحجز !"` | `"حجزت بلاصتك!"` (same as L5's i18n `okTitle`) |
| 〃 | `errUnavailable: "هذه الحصة ما عادش متوفّرة."` | `"الحصة هاذي ما عادش موجودة."` |
| 〃 *(extra)* | `soldOutTitle` / `closedTitle` (same strings as the class page) | same fixes as the class page |
| 〃 *(extra)* | `` `هذه الحصة بـ ${p} د.ت.` `` (L3 A22) | `` `الحصة هاذي بـ ${p} د.ت.` `` |
| `live/[id]/page.tsx` (L3) | `lockedTitle: "هذه الحصة مخصّصة للتلاميذ المحجوزين"` | `"الحصة هاذي للتلامذة اللي حاجزين برك"` |
| 〃 | `tutorNote: "إنت الأستاذ متاع هذه الحصة."` | `"إنت الأستاذ متاع الحصة هاذي."` |
| 〃 *(extra)* | `cancelledTitle: "هذه الحصة تلغات"` (L3 A16, after L5's scan) | `"الحصة هاذي تلغات"` |
| `student/page.tsx` | `liveNow: "المباشر بدا توّا"` | `"الدايركت بدا توّا"` |
| 〃 | `already: "لقد نقّمت هذه الحصة من قبل."` | `"قيّمت الحصة هاذي من قبل."` |
| 〃 | `notBooked: "ما كنتش محجوز في هذه الحصة."` | `"ما كنتش محجوز في الحصة هاذي."` |
| 〃 | `notStarted: "هذه الحصة ما زالت ما صارتش."` | `"الحصة هاذي ما زالت ما صارتش."` |
| `dashboard/new-pack/page.tsx` (L1) | `…أمشي لـ « التثبّت »…` | `…امشي لـ « التثبّت »…` |
| `components/WrongRoleNotice.tsx` (L2) | `genericBody: "حسابك ما عندوش نفاذ لهذه الصفحة."` | `"حسابك ما ينجّمش يدخل للصفحة هاذي."` |
| `app/[locale]/layout.tsx` (site description) | `لقّي أستاذ في المباشر، …` | `لقّي أستاذ دايركت، …` |
| `app/[locale]/page.tsx` | `howTitle: "من أول كليك للدرس المباشر، في 4 مراحل."` | `"من أول كليك للحصة الدايركت، في 4 مراحل."` |
| `signup/eleve/page.tsx` (meta) | `…في الدروس المباشرة متاع أساتذة مؤكّدين.` | `…في الحصص الدايركت متاع أساتذة مؤكّدين.` |
| `student/layout.tsx` (meta) | `دروسك المباشرة الجاية والحجوزات متاعك في Tnajem.` | `حصصك الدايركت الجاية والحجوزات متاعك في Tnajem.` |
| `privacy/page.tsx` (L6) | `برمز وحيد` · `يبعثلك رمز الدخول (… والرمز، وخلاص)` · `رموز الدخول` ×3 · `الرموز المشفّرة` · `تعمّر رمز الدخول` | `بكود وحيد` · `يبعثلك كود الدخول (… والكود، وخلاص)` · `كودات الدخول` ×3 · `الكودات المشفّرة` · `تعمّر كود الدخول` |
| `terms/page.tsx` (L6) | `الإيميل ورمز الدخول` · `وبرمز وحيد` · `الرمز هذا` · `منّك الرمز` | `الإيميل وكود الدخول` · `وبكود وحيد` · `الكود هذا` · `منّك الكود` |
| 〃 | `تسجيل ولا تصوير ولا إعادة بثّ حصة من غير موافقة…` | `تسجيل ولا تصوير حصة، ولا تعاود تنشرها، من غير موافقة…` |
| 〃 | `11. الحصص المباشرة …` · `الحصص المباشرة تنجّم …` | `11. الحصص الدايركت …` · `الحصص الدايركت تنجّم …` |

Every line changed in another lane's file carries `// phase-a lane L6 (A18.derija-2)` (appended with « · » where a lane comment was
already there).

**Suggestions not applied as written, and why:**
- `إعادة بثّ` → L5 suggested « تعاود تبثّ ». The terms list is a series of nouns (تبادل، مضايقة، نشر، انتحال، تسجيل ولا تصوير…), so a
  finite verb breaks it, and « بثّ » is itself broadcast/MSA vocabulary. Used « حصة، ولا تعاود تنشرها » (Tunisian « تنشر »).
- `رموز الجلسة` (session *tokens*, not the login code) and `بالرمز روحو` (the session token) are left as « رمز »: L5's fix targets the
  login code (« الكود »); a token is not a code a person types.
- `مباشرة` meaning *directly* (privacy « مباشرة من « حسابي » ») and `غير المباشرة` (*indirect* damages) are not the live-class word; kept.

**Skipped:** `consent.body` (consent wording, LEGAL-REVIEW). **Already fixed before this lane:** dashboard `منذ` (L5), StorefrontView
`مجانية` / `المباشرة` / `دواتم` (L5), checkout « هذه الحصة مقدّمة مجاناً… » (L3 rewrote the free line for A6: « فابور كان هي أول حصة ليك مع هالأستاذ »).
**Left (not in L5's table, other lanes' file):** `dashboard/page.tsx` `ffTitle: "الحصة الأولى مجانية"` and its body; the class page's
« إلغاء مجاني ». No e2e spec or API test asserted any of the changed Arabic strings (grep of `e2e/`, `apps/api/test/`, `tools/`).

Files touched: the 12 web files in the table, `apps/api/test/l6-derija.test.ts` (new).

---

## Claims on the pages that could NOT be made fully true (left as LEGAL-REVIEW / flagged)

1. **Retention periods** — none exists in the code for consents, messages, hidden content, reports, bookings, the verification trace or
   backups; the pages say what the code does and keep « [Durée à fixer par l'avocat] » (LEGAL-REVIEW). Not invented.
2. **The guardian passages** (§3 parent e-mail, §4, §7, §8) describe code that is switched off during the pilot (A24) and incomplete:
   nothing creates a parent account (CEO finding 3 / Dm1) — the text now says the consent is linked to « le compte que le parent ouvre
   avec cette adresse » — and the parent has no report button (Dm2, not mentioned). All of it sits under « ne s'appliquent pas tant que
   cet accès est fermé ». **Must be rewritten when Phase D opens minors.**
3. **Payments during the pilot** — the off-platform arrangement paragraph is deleted (D4); whether the terms need any clause about how a
   tutor is paid while Tnajem collects nothing is a founder/counsel question (LEGAL-REVIEW comment on terms §6).
4. **ID documents: "au plus tard 90 jours après la décision"** — true per dossier, but retention.ts runs the clock from the *latest*
   decision, so an earlier round's scans wait for the newer round's decision. Now stated on the page (LEGAL-REVIEW: per-document clock?).

## Findings outside my files (not changed — for the orchestrator/founder)

- **Class page** (`class/[id]/page.tsx`, L4): the « 1ère séance gratuite » reassurance is shown whenever `cls.is_free_first`, even to a
  student who already used their free session with this tutor (A6 made it once per student per tutor; checkout already says « Offerte
  si c'est ta 1ʳᵉ séance avec ce prof »). Truth-rule risk.
- **`POST /reports` accepts `subjectKind: "message"` without an account** when the caller has the message id. No UI offers it; ids are
  visible to participants and to a linked guardian. Not a leak of content, but it is a path the pages do not describe (and a cheap
  starting point for Dm2).
- **Consent form** (`auth/consent`, L5 A18.1): « Ton parent recevra un e-mail pour confirmer » — `POST /consent` sends no e-mail (no
  `sendMail` there). Behind `ALLOW_MINORS`, so invisible in the pilot; it describes Dm3.
- **Site description** (`app/[locale]/layout.tsx`, FR/AR): « du primaire au bac » / « من الابتدائي للباك » during an adults-only pilot.
- A hidden review keeps its **rating** in the average (L4 judgement call 4); the pages say only that its *text* is replaced — accurate.

## Must-not-break (§2)

No application logic changed in this lane: only copy (two legal pages, AR strings in 10 web files), the two version constants in
`legal.ts`, and tests. Evidence from the final `npm run test:api` (567/567): seat booking atomic — `booking-errors.test.ts` "a genuine
concurrent double-submit is still idempotent", `fx-smoke.test.ts`; guardian consent server-side — `a24-adult-pilot.test.ts` "with
ALLOW_MINORS=1 the guardian gate is intact"; ID scans — `doc-security.test.ts`; admin gate — `admin.test.ts` "isAllowlistedAdmin — fails
closed on every path"; erasure/purge — `avatar-erasure.test.ts`, `consent-erasure.test.ts`, `verified-rename-resubmit.test.ts`;
cancellation 47h59/48h00 — `cancellation.test.ts`; Africa/Tunis — `time.test.ts`; no contact field — `contact-info.test.ts`
(`contactFieldPaths`); no invented ratings — `standing.test.ts`; demo locks — `boot-posture.test.ts`; schema = migrations —
`npm run db:check`: `✓ migrations — 31 files through 0030_consent_phone_optional.sql: 30 tables and 37 added columns present` ·
`✓ OK, 1 warning(s)` (the warning is an env key). No migration in this lane.

## Files outside my ownership I touched (merge hints)

- `packages/shared/src/legal.ts` — the two version constants + their comments (not an add-only file; two lines).
- `apps/api/test/payment-story.test.ts` (L3) — `EXCLUDED` no longer lists /terms and /privacy (marked `phase-a lane L6 (A19)`).
- `e2e/privacy-documents.spec.ts` — version date + one test title (marked).
- Derija: `class/[id]/page.tsx`, `checkout/CheckoutInner.tsx`, `live/[id]/page.tsx`, `student/page.tsx`, `dashboard/new-pack/page.tsx`,
  `components/WrongRoleNotice.tsx`, `app/[locale]/layout.tsx`, `app/[locale]/page.tsx`, `signup/eleve/page.tsx`, `student/layout.tsx` —
  AR string values only, each line marked.

New exports: none (the `story` field is page-local). New env vars: none. Migrations: none.

## FOUNDER defaults used / decisions to confirm

- `THREAD_CLOSE_DAYS = 7` (L1's FOUNDER default) is what the pages quote — imported, never typed in.
- `PRIVACY_POLICY_VERSION` / `TERMS_VERSION` = `2026-09-24` (was `2026-09-15`).
- The payment paragraph is labelled future (« Bientôt ») on both pages; nothing names a provider.

## Final gates (HEAD `23ddd30`, before this log)

```
npm run typecheck   → exit 0 (@tnajem/api, @tnajem/web, @tnajem/db, @tnajem/shared tsc --noEmit + tsc --noEmit -p e2e)
npm run lint        → exit 0 (eslint . --max-warnings=0)
npm run test:api    → # tests 567 · # suites 130 · # pass 567 · # fail 0 · # cancelled 0 · # skipped 0
                      (542 at baseline + 13 legal-truth + 12 l6-derija)
npm run db:check    → ✓ migrations — 31 files through 0030_consent_phone_optional.sql: 30 tables and 37 added columns present
```
Playwright: not run by this lane (another process holds the machine's Playwright gate). Written: `e2e/legal-truth.spec.ts`.
Updated: `e2e/privacy-documents.spec.ts`.
